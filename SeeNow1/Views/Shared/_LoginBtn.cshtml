@using Microsoft.AspNetCore.Http
@{ 
    var loginName = @Context.Session.GetString("_Name");
    }
@*<form class="nav navbar-form navbar-right">*@
<div class="nav navbar-right">
    <ul class="nav navbar-nav">
        @*<li>
            <a href="@Url.Action("Chat", "Home")"><span class="glyphicon glyphicon-send">Chat</span></a>
        </li>*@
        @{ if (@loginName != "")
            {
                @*<li><a href="@Url.Action("Chat", "Home")"><span class="glyphicon glyphicon-user" span style="color:brown">@ViewBag.userName</span></a></li>*@
                <li>
                    <a href="@Url.Action("Chat", "Home")" style="color:brown">Hello @loginName</a>
                </li>
                <li></li>
                <li>
                    <a href="@Url.Action("Logout","Home")" style="border:2px solid;border-radius:5px;">登出</a>
                </li>
            }
            else
            {
                <li><a href="@Url.Action("SqlLoginForm", "Home")"><span class="glyphicon glyphicon-log-in">登入</span></a></li>
            }
        }

        @*Search icon on a button:
            <button type="button" class="btn btn-default">
                <span class="glyphicon glyphicon-search">Search</span>
            </button>*@
        @*<button type="button" class="btn btn-default btn-sm">
                <span class="glyphicon glyphicon-log-in"></span> Log in
            </button>*@
    </ul>
</div>
@*</form>*@